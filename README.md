# react-exp14-fetch-rest-api
To integrate an external REST API into a React app using **fetch()** or **Axios**, display the data in a table, and handle both **loading** and **error states** effectively.
